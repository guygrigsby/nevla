import py "threading"

fn main() (error?) {
    l := check threading.Lock()
    for i := range 3 {
        with l {
            print(i)
            break
        }
    }
    print(check bool(l.locked()))
    return none
}
